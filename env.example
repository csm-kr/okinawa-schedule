# 환경 변수 템플릿 — 값은 비워 둔다(키만). 비밀 값은 절대 커밋하지 않는다.
# 로컬: STORAGE_DRIVER=memory 로 KV 없이 동작. 배포(Vercel): STORAGE_DRIVER=upstash + 토큰 주입.

STORAGE_DRIVER=upstash
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
ADMIN_PASSWORD=
ADMIN_COOKIE_SECRET=
